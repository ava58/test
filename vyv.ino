void vyv(byte xpoz,byte cif){
  switch (cif) {
        case 0:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c0[x][0]);avaPoint(xpoz+1,x,c0[x][1]);avaPoint(xpoz+2,x,c0[x][2]);
        }
       break;
        case 1:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c1[x][0]);avaPoint(xpoz+1,x,c1[x][1]);avaPoint(xpoz+2,x,c1[x][2]);
        }
       break;
        case 2:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c2[x][0]);avaPoint(xpoz+1,x,c2[x][1]);avaPoint(xpoz+2,x,c2[x][2]);
        }
       break;
        case 3:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c3[x][0]);avaPoint(xpoz+1,x,c3[x][1]);avaPoint(xpoz+2,x,c3[x][2]);
        }
       break;
        case 4:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c4[x][0]);avaPoint(xpoz+1,x,c4[x][1]);avaPoint(xpoz+2,x,c4[x][2]);
        }
       break;
        case 5:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c5[x][0]);avaPoint(xpoz+1,x,c5[x][1]);avaPoint(xpoz+2,x,c5[x][2]);
        }
       break;
        case 6:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c6[x][0]);avaPoint(xpoz+1,x,c6[x][1]);avaPoint(xpoz+2,x,c6[x][2]);
        }
       break;
        case 7:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c7[x][0]);avaPoint(xpoz+1,x,c7[x][1]);avaPoint(xpoz+2,x,c7[x][2]);
        }
       break;
        case 8:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c8[x][0]);avaPoint(xpoz+1,x,c8[x][1]);avaPoint(xpoz+2,x,c8[x][2]);
        }
       break;
        case 9:
          for(int x=0;x<8;x++){//вывод цифры в заданную позицию
          avaPoint(xpoz,x,c9[x][0]);avaPoint(xpoz+1,x,c9[x][1]);avaPoint(xpoz+2,x,c9[x][2]);
        }
       break;                     
  }
}
